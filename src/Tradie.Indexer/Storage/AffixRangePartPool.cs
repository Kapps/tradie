using System.Runtime.InteropServices;

namespace Tradie.Indexer.Storage;

public unsafe class AffixRangePartPool {
	/*public ref AffixRange Acquire() {
		if(_available.Count > 0) {
			ref var res = ref _available[^-1];
		}
	}

	private AffixRange* Allocate(ulong modKind, float minValue, float maxValue) {
		void* ptr = Marshal.AllocHGlobal(Marshal.SizeOf<AffixRange>());
	}

	private ArraySegment<AffixRange> _available;
	private int _capacity;*/
}